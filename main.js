let a = document.getElementById('img');
let container = document.getElementById('container');// Define the 'container' variable

function phones(phone) {
    a.src = phone;
}

function colors(color) {
    container.style.background = color; // Update the background color of the 'container'
}
